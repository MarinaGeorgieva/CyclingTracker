<Page xmlns="http://schemas.nativescript.org/tns.xsd" xmlns:drawer="nativescript-telerik-ui/sidedrawer" loaded="pageLoaded">
  <Page.actionBar>
  	<ActionBar title="Cycling Tracker">
      <ActionBar.titleView>
        <StackLayout orientation="horizontal" ios:horizontalAlignment="center" android:horizontalAlignment="left">
          <Image src="res://icon" class="action-image"/>
          <Label text="{{ username }}"  class="action-label"/>
        </StackLayout>
      </ActionBar.titleView>  
    </ActionBar>
  </Page.actionBar>
  <drawer:RadSideDrawer>
  	<drawer:rad-side-drawer.drawerTransition>
        <drawer:PushTransition />
    </drawer:rad-side-drawer.drawerTransition>
    <drawer:RadSideDrawer.mainContent>
    	<ScrollView>
    		<StackLayout>
          <Image  id="profileImage" src="{{ profilePictureUrl }}" class="profile-picture"/>
          <Label text="{{ fullName }}" />
          <Label text="My tours" id="myTours"/>
          <ListView items="{{ tours }}" itemTap="tourItemTapped">
            <ListView.itemTemplate>
              <StackLayout>
                <Image src="{{ trackPictureUrl }}" height="90"/>
                <!-- <Label text="{{ distance }}" /> -->
                <!-- <Label text="{{ createdAt }}" /> -->
              </StackLayout>
            </ListView.itemTemplate>
          </ListView>
        </StackLayout>
    	</ScrollView>
    </drawer:RadSideDrawer.mainContent>
    <drawer:RadSideDrawer.drawerContent>
      <StackLayout>
    		<Button text="Track" tap="tapTrack" />
    		<Button text="Profile" tap="tapProfile" />
    		<Button text="Shared tracks" tap="tapShared" />
     	</StackLayout>
    </drawer:RadSideDrawer.drawerContent>
  </drawer:RadSideDrawer>
</Page>